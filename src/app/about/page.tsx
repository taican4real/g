import { PublicPage } from "../components/public-page";

export default function AboutPage() { return <PublicPage><section className="page-section"><span className="eyebrow">About ExamForge</span><h2>A dependable foundation for tutorial centres.</h2><p>ExamForge is being built around the real operating needs of Nigerian tutorial centres: clear roles, centre-owned access, examination configuration, and learning workflows that can be trusted.</p><div className="feature-grid"><div className="feature"><h3>Centre-first</h3><p>Administrators, teachers, and students see the work relevant to their centre.</p></div><div className="feature"><h3>Secure by default</h3><p>Authentication and authorization are enforced on the server, with tenant isolation at the database boundary.</p></div><div className="feature"><h3>Evidence over noise</h3><p>Progress and results will be based on recorded learning activity, not invented dashboard numbers.</p></div></div></section></PublicPage>; }
